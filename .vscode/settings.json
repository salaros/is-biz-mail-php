{
    // PHP CodeSniffer settings
    "phpcs.enable": true,
    "phpcs.standard": ".vscode/phpcs.xml",

    // Exclude files from File Explorer
    "files.exclude": {
        ".vs":              true,
        "vendor":           true,
        "src/dotnet/obj":   true,
        "test/dotnet/obj":  true,
        "node_modules":     true,
    },
}